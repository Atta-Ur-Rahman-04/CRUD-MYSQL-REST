# CRUD Operations Using REST API with Node.js & MySQL

A backend REST API built with **Node.js** and **Express.js** that performs full
CRUD (Create, Read, Update, Delete) operations using a **MySQL** database.
